@* see comments at top of Index.cshtml *@
@model Guide

<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
</head>
<body>
  <h2>Greetings @Model.Traveler.FirstName @Model.Traveler.LastName. Where would you like to travel to?</h2>

  <ul>
    @foreach (Destination destination in Model.Destinations)
    {   
        @* since we are inside a code block we can write C# code *@
        string test = "test";

        @* As soon as we write HTML, we cannot write C# anymore unless we use the special template rendering syntax *@
        <li>
          <a href="/travel/@destination.Name">@destination.Name</a>
        </li>
    }
  </ul>
</body>
</html>